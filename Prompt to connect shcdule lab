Add a schedule-style config to my dashboard.

I have a style config JSON copied from a "Schedule Lab" tool. Wire my dashboard so my schedule/tasks render with this exact style:

- Read the style from localStorage['peak_sched_style_v1'] if it exists; otherwise use the config below as the baked-in default.
- Apply all of its values — band fill, opacity, corner radius, color mode, accent, pill style/position, and the difficulty colors — to how my task bands and chips are drawn.
- If my dashboard doesn't draw schedule task bands over a chart yet, build that too.

Here is my config:

<paste your Copy config here>
